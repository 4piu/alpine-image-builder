# U-Boot config fragment for this target, merged onto the board's stock
# defconfig -- see target/README.md. Empty (comment-only) has no effect.
#
# Add lines like:
#   CONFIG_SOME_OPTION=y
#   # CONFIG_OTHER_OPTION is not set
#
# Or capture changes interactively instead of hand-writing them:
#   make uboot-menuconfig TARGET=zeropi
